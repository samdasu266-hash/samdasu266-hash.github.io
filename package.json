{
  "name": "health-jobs-portal",
  "private": true,
  "description": "보건공기업 알리미 - 정적 빌드 스크립트",
  "scripts": {
    "build:js": "babel app.jsx -o app.js",
    "build:css": "tailwindcss -i tailwind.input.css -o tailwind.css --minify",
    "build": "npm run build:js && npm run build:css"
  },
  "devDependencies": {
    "@babel/cli": "^8.0.4",
    "@babel/core": "^8.0.1",
    "@babel/preset-react": "^8.0.1",
    "tailwindcss": "^3.4.19"
  }
}